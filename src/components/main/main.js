import Grid from './grid/grid';
import Banner from './banner/banner';

export default function Main() {
    return (
        <main>
            <Grid></Grid>
            <Banner></Banner>
        </main>
    );
}